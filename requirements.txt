feedparser>=6.0
httpx>=0.27
PyYAML>=6.0
Pygments>=2.17
beautifulsoup4>=4.12
lxml>=5.0
spacy>=3.7
# After installing: python -m spacy download en_core_web_sm
nltk>=3.8
# Prerender step (make assemble) drives headless Chromium to snapshot the JS
# reader's output into the page for no-JS readers + crawlers.
# After installing: python -m playwright install chromium
playwright>=1.40
